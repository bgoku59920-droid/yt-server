services:
  - type: web
    name: yt-stream-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:10000 --workers 2 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
